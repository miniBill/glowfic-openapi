{
    "license": "BSD-3-Clause",
    "scripts": {
        "start": "watchexec -c -w glowfic-openapi.yaml --on-busy-update restart 'bun run gen; bun elm-pages dev --keep-cache'",
        "build": "bun run gen && elm-pages build",
        "gen": "elm-pages run ./script/src/GenerateOpenApi.elm"
    },
    "devDependencies": {
        "elm": "^0.19.1-6",
        "elm-optimize-level-2": "^0.4.0",
        "elm-pages": "^3.5.1",
        "elm-review": "^2.13.5",
        "lamdera": "0.19.1-1.3.2"
    }
}
